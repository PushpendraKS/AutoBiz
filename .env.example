# Option 1: absolute or workspace-relative path to your local service-account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=./service.json

# Option 2: paste the minified service-account JSON into this variable (recommended for Vercel)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

PORT=3000
